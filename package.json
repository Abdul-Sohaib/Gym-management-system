{
  "name": "gym-client-hub",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "typecheck": "npm run typecheck --prefix artifacts/api-server && npm run typecheck --prefix artifacts/gym-app",
    "build": "npm run build --prefix artifacts/api-server && npm run build --prefix artifacts/gym-app",
    "dev": "npm run dev --prefix artifacts/gym-app",
    "dev:api": "npm run dev --prefix artifacts/api-server"
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  },
  "dependencies": {
    "esbuild-plugin-pino": "^2.3.3"
  }
}
